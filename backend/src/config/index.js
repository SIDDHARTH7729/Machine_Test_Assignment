// This file acts as a central index file for exporting all configurations used in the application.
const ServerConfig = require('./serverConfig');

// expoting the config
module.exports = {
    ServerConfig
}